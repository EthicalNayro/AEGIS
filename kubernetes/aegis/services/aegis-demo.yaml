apiVersion: v1
kind: Service
metadata:
  name: aegis-demo
  namespace: aegis-system
  labels:
    app.kubernetes.io/name: aegis-demo
    app.kubernetes.io/part-of: aegis

spec:
  type: ClusterIP

  selector:
    app.kubernetes.io/name: aegis-demo

  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
