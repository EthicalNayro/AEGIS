---
- name: Reload systemd
  ansible.builtin.systemd:
    daemon_reload: true

- name: Restart Status-Page services
  ansible.builtin.systemd:
    name: "{{ item }}"
    state: restarted
  loop:
    - status-page
    - status-page-scheduler
    - status-page-rq
